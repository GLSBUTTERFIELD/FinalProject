import { GatheringParticipant } from './gathering-participant';

describe('GatheringParticipant', () => {
  it('should create an instance', () => {
    expect(new GatheringParticipant()).toBeTruthy();
  });
});
