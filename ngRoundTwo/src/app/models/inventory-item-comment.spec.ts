import { InventoryItemComment } from './inventory-item-comment';

describe('InventoryItemComment', () => {
  it('should create an instance', () => {
    expect(new InventoryItemComment()).toBeTruthy();
  });
});
